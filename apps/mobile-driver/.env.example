# Leave these blank in local Expo development so the app can auto-detect
# your dev machine from the Expo host. Set them only for a fixed API host.
EXPO_PUBLIC_API_BASE_URL=
EXPO_PUBLIC_SOCKET_URL=
EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=
