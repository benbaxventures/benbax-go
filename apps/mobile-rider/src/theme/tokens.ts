import { colors, radius, spacing } from '@benbax/shared';

export const theme = {
  colors,
  radius,
  spacing,
  shadow: {
    shadowColor: '#111827',
    shadowOpacity: 0.08,
    shadowRadius: 12,
    shadowOffset: { width: 0, height: 6 },
    elevation: 3
  }
};
